"use client";
import Link from "next/link";
import { usePathname } from "next/navigation";
const sections = [{ href: "/", label: "Главная", icon: "⌂" }, { href: "/transactions", label: "Операции", icon: "↕" }, { href: "/budgets", label: "Бюджет", icon: "◴" }, { href: "/settings", label: "Настройки", icon: "⚙" }] as const;
function current(pathname: string, href: string) { return href === "/" ? pathname === href : pathname.startsWith(href); }
export function AppNavigation({ variant }: Readonly<{ variant: "mobile" | "desktop" }>) { const pathname = usePathname(); return <nav aria-label="Основная навигация"><ul className={variant === "mobile" ? "grid grid-cols-4" : "space-y-1.5"}>{sections.map((item) => { const active = current(pathname, item.href); return <li key={item.href}><Link href={item.href} aria-current={active ? "page" : undefined} className={variant === "mobile" ? `flex min-h-14 flex-col items-center justify-center gap-0.5 rounded-xl text-[11px] font-semibold transition ${active ? "text-[var(--accent)]" : "text-[var(--text-muted)]"}` : `flex min-h-11 items-center gap-3 rounded-xl px-3.5 text-sm font-semibold transition ${active ? "bg-[var(--accent-soft)] text-[var(--accent)]" : "text-[var(--text-muted)] hover:bg-[var(--surface-muted)] hover:text-[var(--text)]"}`}><span aria-hidden className={variant === "mobile" ? "text-lg leading-none" : "grid size-7 place-items-center text-lg"}>{item.icon}</span>{item.label}</Link></li>; })}</ul></nav>; }
