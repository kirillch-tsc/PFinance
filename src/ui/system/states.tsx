import type { ReactNode } from "react";
import { Button } from "./button";
import { Card } from "./surfaces";
export function EmptyState({ title, message, action }: Readonly<{ title: string; message: string; action?: ReactNode }>) { return <Card className="border-dashed p-8 text-center sm:p-12"><div className="mx-auto mb-4 grid size-12 place-items-center rounded-2xl bg-[var(--accent-soft)] text-xl text-[var(--accent)]" aria-hidden>◇</div><h2 className="text-xl font-bold tracking-tight text-[var(--text)]">{title}</h2><p className="mx-auto mt-2 max-w-md text-sm leading-6 text-[var(--text-muted)]">{message}</p>{action ? <div className="mt-5">{action}</div> : null}</Card>; }
export function Loading({ label = "Загрузка…" }: Readonly<{ label?: string }>) { return <div role="status" className="flex min-h-32 items-center justify-center gap-3 text-sm text-[var(--text-muted)]"><span className="size-5 animate-spin rounded-full border-2 border-[var(--border-strong)] border-t-[var(--accent)]" />{label}</div>; }
export function ErrorState({ title = "Не удалось загрузить данные", message, onRetry }: Readonly<{ title?: string; message: string; onRetry?: () => void }>) { return <Card className="p-7 text-center"><h2 className="text-lg font-bold text-[var(--danger)]">{title}</h2><p role="alert" className="mt-2 text-sm text-[var(--text-muted)]">{message}</p>{onRetry ? <Button variant="secondary" onClick={onRetry} className="mt-5">Повторить</Button> : null}</Card>; }
